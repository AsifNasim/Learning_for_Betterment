#include<stdio.h>
#include<limits.h>
#include<math.h>

int main(){
    // int a;
    // float f;
    // double dl;
    // char ch;

    // scanf("%d %f %lf %c", &a, &f, &dl, &ch);
    // printf("%d %.2f %.4lf %c", a, f, dl, ch);

    char ch;
    int x, y;
    scanf("%c %d %d", &ch, &x, &y);
    printf("%c %d %d", ch, x, y);
    return 0;


    
}

// how to take I/p from user in a console
// Scanf
// 1. User types his I/p in console.
// 2. We assign the typed value to the corresponding datatypes
// Address of the variable
// scanf continues to scan a part var till it reaches a blank space



